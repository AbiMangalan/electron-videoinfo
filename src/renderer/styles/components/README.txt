# Component Styles Directory

This directory contains CSS styles specific to UI components.

Each component style should:
- Match the name of its corresponding component
- Only style its specific component
- Use scoped selectors to avoid affecting other components